# 数学地图（按数学本身）

**这版按数学的内在依赖分，不按目标、用途、兴趣分。旧 A/B/C/D 作废。**
学不动就是前置没过，回去补，不换线逃。

- 逐次记录 → `学习日志.md`
- 开始：2026-08-05 ｜ 本图重写：2026-09-12（按数学分支，旧目标口径作废）
- 进度坐标见 §9，判据不变：能自己说清为什么，才算过。

---

## 0. 数学长什么样

```
I   数系与运算 ──┐
II  代数 ────────┤
III 几何 ────────┼──→ V 分析 ──→ VII 概率统计
IV  三角 ────────┘         ↑
VI  线性代数 ──────────────┘（多元分析、统计都要它）
VIII 逻辑与证明：贯穿，不单独成关，最后收口
```

依赖是硬的：V 要 I+II，VI 要 II，VII 要 II+V（三），VIII 什么时候较真什么时候补。

---

## I. 数系与运算

自然数 → 整数 → 有理数 → 实数 → 复数（先到实数即可，复数后置）。
核心：运算律、分数、指数、对数、绝对值、不等式。

- 前置：无。
- 过关：分数与指数运算不出错；`log` 知道是指数的逆；不等式变号不犯错。
- 你的位置：✅ 基本可用。短板只剩符号熟练度（幂函数系数要停下来推），碎片时间练，不单开一章。

## II. 代数

核心：把未知数当对象，用恒等变形解约束。

1. 整式、分式、因式分解
2. 一元一次/二次方程，不等式
3. 函数语言：定义域、对应、图像、单调、奇偶（这是 V 的门票）
4. 二元方程组：消元、交点、无解/无穷解
5. 三元消元：相加保解
6. 多项式余式、复数入门（后置）

- 过关：三元能独立消元并说清“左边加左边、右边加右边，解不丢”；二次方程判别式与图像对应上。
- 你的位置：✅ 1–5 已过（2026-09-11：`x=1,y=2,z=3` 独立解出；二元秒解并说清平行/重合）。

## III. 几何

欧氏几何 → 相似与比例 → 圆 → 解析几何（坐标+方程）→ 立体入门。

- 前置：I。
- 过关：相似能算够不着的距离；圆面积能用极限切出来（你做过 `πr²`）；直线/圆方程与图像互译。
- 你的位置：🔵 半过。用极限切过圆面积，相似、解析几何没系统走。这是欠账，不是“零散”，要补。

## IV. 三角

角度 → 弧度 → 单位圆 → sin/cos/tan → 恒等式 → 波与周期。

- 前置：III（相似+圆）。
- 过关：在单位圆上读出任意角 sin/cos，说清“投影”；解释周期 `2π` 从哪来；证出 `sin²+cos²=1`。
- 你的位置：⬜ 未开始。这是 V 后半段和信号的门票，别拖太久。

## V. 分析（函数 → 极限 → 微分 → 积分 → 级数 → 方程 → 多元）

数学的主干，也是你走得最远的一支。

- V1 函数与图像 ✅：比例、一次、二次、指数对数，能画形说快慢拐点。
- V2 极限 ✅：亲手做过 `((4+h)²-4²)/h=8+h → 8`。
- V3 微分 ✅：多项式秒求导；`导数=0`只是候选，变号才是极值（`t³` 在 0 处平一下不是极值）；围栏约束优化独立走完 `50m²`。
- V4 积分 ✅（2026-09-11）：`S(3)=9` 总量与 `v(3)=6` 速度分清；`S(T)=T² → S'=2T=v(T)` 自己写出；8 矩形估算知高估低估。
- V5 级数与逼近 ⬜：Taylor——函数写成多项式之和，误差可控。什么时候来：现在。V4 过了就该它。
- V6 微分方程 ⬜：已知变化规律反推函数。什么时候来：V5 之后。
- V7 多元与向量微积分 ⬜：梯度、偏导。什么时候来：VI3 之后。不到不碰。

## VI. 线性代数（方程组 → 向量空间 → 矩阵 → 特征值）

- VI1 方程组与行列式 ✅（2026-09-11）：消元+三种解的几何；`2×2` 行列式=两边张成的平行四边形面积（`3×2-0×1=6`，`4×3-0×2=12`），顶点是原点、`u` 尖、`v` 尖、`u+v` 尖。
- VI2 向量与空间 🔵 下一站：线性相关、张成、基、维数、坐标与换基。判据：能判相关并说出为什么；能讲清同一向量两套坐标怎么换。
- VI3 矩阵=变换 ⬜：矩阵是动作不是表格；乘法=复合所以不可交换；行列式为 0=压扁=不可逆。
- VI4 特征值 ⬜：方向不变只拉伸的方向。判据：`2×2` 手算特征值并在图上指出拉伸几倍。
- VI5 二次型与最优 ⬜：最小二乘=投影，正定=碗形。B 与 V 的接缝。

## VII. 概率与统计

计数与概率 → 随机变量与分布 → 期望方差 → 大数定律与中心极限 → 统计推断 → 回归与随机过程。

- 前置：II + V4（积分在连续分布里要用到）。
- 你的位置：⬜ 未开始。前置已够（V4 已过），随时可开，但按顺序先走 VI2 和 V5。

## VIII. 逻辑与证明

集合、命题、量词、直接/反证/归纳、实分析（深水区）。

- 不单独排队，碰到“凭什么”就补一块。E3 实分析只在你追问“什么条件下才成立”时开。
- 你的位置：会因果推理，用过极限，没学量词与写法。

---

## 9. 你现在的坐标（2026-09-11 实测）

| 分支 | 位置 | 状态 |
|---|---|---|
| I 数系 | 可用 | ✅，符号手艺碎片练 |
| II 代数 | 三元及以前 | ✅ |
| III 几何 | 圆面积切过 | 🔵 欠账：相似、解析 |
| IV 三角 | 未开始 | ⬜ |
| V 分析 | V4 积分 | ✅ V1–V4；下一站 V5 级数 |
| VI 线代 | VI1 | ✅；下一站 VI2 向量与空间 |
| VII 概率统计 | 未开始 | ⬜，前置已够 |
| VIII 逻辑证明 | 零散 | 用过极限，无量词写法 |

已确认的能力：图像/斜率/面积直觉真通；求导、极值、约束优化独立走完；消元保解与行列式面积能自己讲。
已暴露的短板：符号代数熟练度。熟练度问题，每天几分钟，不开专题。

## 10. 下一步（按数学顺序，不给四个选项）

1. **VI2 向量与空间**：线性相关、基与坐标、换基。这是主线，不许跳。
2. **V5 级数**：Taylor，第一块硬分析，VI2 之后立刻上。
3. **碎片**：幂函数求导秒出（`(2t³+4t²-1)'=6t²+8t` 不许停）。
4. III 和 IV 的欠账记着，但不插队：VI2+V5 走完再回头系统补相似→弧度→三角。

## 11. 规矩（大胆版）

1. 先直觉再符号再诊断，不刷题。
2. 一次一站，卡住就定位到具体哪句话，把前置补上，不许换线安慰自己。
3. 通关凭“能解释为什么”，写进日志，不是做完题数。
4. 每段记三行：懂了什么 / 哪句还模糊 / 下次从哪句继续。
5. 这张图按数学改，下次改只因数学依赖写错，不因状态不好改。
