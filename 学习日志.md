# 学习日志

- 路线图与过关判据 → `数学地图.md`
- 记录格式：每次三行 —— ① 今天理解了什么　② 哪一步仍然模糊　③ 下一次从哪里继续

## 进度快照（2026-09-11 复核更新）

- **A 变化线**：微分复核通关（2026-09-11），积分已通关。原卡点“S'(T)=v(T)”已破：$S(3)=9$（总量）与 $v(3)=6$（速度）已分清，$S'(T)=2T$ 自己写出。
- 卡点复盘（2026-09-11）：曾把平均速度里的 $/2$ 算两遍（$v(1.5)=3$ 已是平均，再除 2 得 4.5）；已统一三角形与平均两种算法都是 9。
- **B 结构线**：第 1 站进行中。二元方程组秒解（$x=5,y=5$），三种解情形的几何图景一次答对；待三元消元与行列式。
- **C 不确定线**：未开始。
- **D 形状线**：零散 —— 用扇形切割加极限算过圆面积 $\pi r^2$，相似、弧度、正弦余弦未系统学。
- **E 严格线**：零散 —— 用过极限，没学量词与证明写法。
- 已确认能力：图像与斜率面积直觉、求导规则、极值流程、约束优化建模。
- 已暴露短板：符号代数熟练度（幂函数与系数的求导需停下来推）。

---

## 学习记录

### 第一次学习：从面积与斜率到优化（2026-08-05）

**阶段 1：速度—时间图的面积就是路程**

- 理解了匀速运动时，速度—时间图下面的矩形面积就是路程。
- 正确判断匀加速案例的图形是三角形，并得到路程 $\frac12\times3\times4=6$ 米。
- 正确计算梯形案例：$\frac{2+6}{2}\times4=16$ 米，并主动发现可用平均速度 $4\times4$ 计算。
- 理解曲线下面积的近似：把区域切得越精细，矩形组成的“锯齿”就越贴近曲线边界，估算也越准确。

**阶段 2：路程—时间图的斜率就是速度**

- 正确理解路程—时间图的斜率表示速度：每秒增加 3 米对应速度 $3\text{ m/s}$。
- 需要辨析：累计路程常把出发时刻设为 0，但“速度等于斜率”取决于路程变化量与时间变化量，不取决于图像是否从原点开始。
- 进一步辨析：斜率本身保留正负号；位置—时间图的斜率是速度，速度大小才是其绝对值。累计路程—时间图通常不会下降，因此斜率通常非负。
- 当前进度：已经建立了速度—时间图的面积表示路程、路程—时间图的斜率表示速度的直觉。
- 下一步：从平均速度进入某一瞬间的速度，理解切线斜率与导数。

**阶段 3：从平均斜率到瞬时斜率（极限）**

- 导数练习中的待巩固点：要区分位置值 $s(t)$ 与速度（位置对时间的变化率）；平均速度应计算为 $\frac{s(b)-s(a)}{b-a}$，不是两个位置值的平均数。
- 理解平均斜率公式的来源：取两点 $(4,4^2)$ 与 $(4+h,(4+h)^2)$，用纵坐标变化量除以横坐标变化量，得到 $\frac{(4+h)^2-4^2}{h}$。
- 理解展开并约去非零的时间间隔后，平均斜率变为 $8+h$，令 $h\to0$ 得到瞬时斜率 8。
- 下一步：把这个过程推广到任意时刻，得到 $\frac{d}{dt}(t^2)=2t$。
- 正确应用 $\frac{d}{dt}(t^2)=2t$：判断 $t=5$ 时瞬时速度为 10，并开始感受到导数公式背后的规律性。
- 明白“求导就是求曲线某点切线的斜率”，并能用 $y'=2t$ 读取 $y=t^2$ 在不同位置的切线斜率。
- 下一步：理解导数的正负与函数上升、下降、极值之间的关系。

**阶段 4：极值——导数为 0 只是候选**

- 正确判断 $y=t^2$ 在 $t=0$ 取得唯一的全局最小值；理解其左侧导数为负、右侧导数为正。
- 注意：导数等于 0 只是极值候选条件，还要观察导数符号是否发生变化。
- 理解对称例子 $y=-t^2$：导数 $-2t$ 在左侧为正、右侧为负，因此 $t=0$ 是最高点。
- 下一步：整理常数、一次函数、系数和幂函数的基础求导规则。
- 正确应用基础求导规则：$\frac{d}{dt}(4t^2-3t+7)=8t-3$。
- 下一步：从 $t^2$ 推广到 $t^3$ 和一般幂函数规则。
- 幂函数练习中需要巩固：系数要保留并乘上指数；$(4t^2)'=4\cdot2t=8t$，因此 $(2t^3+4t^2-1)'=6t^2+8t$。
- 下一步：把求导用于求极值，掌握“求导 → 令导数为 0 → 检查导数符号 → 求函数值”的流程。
- 正确应用极值流程：对 $f(t)=t^2+6t+5$ 得到 $t=-3$，函数值为 $-4$，最低点为 $(-3,-4)$。
- 下一步：把导数求极值与二次函数配方联系起来，并继续练习最大值问题。
- 正确处理开口向下的二次函数：对 $g(t)=-t^2+6t+1$ 得到最高点 $(3,10)$，理解负号使导数由正变负、函数由上升转为下降。
- 下一步：用固定资源的实际问题练习优化，把文字条件翻译成函数，再用导数找最大值或最小值。

**阶段 5：约束优化——围栏问题（A3 通关证据）**

- 优化问题中正确变形约束：由 $2x+y=20$ 得到 $x=\frac{20-y}{2}$，并能改写为后续更方便的 $y=20-2x$。
- 正确建立面积函数 $A(x)=20x-2x^2$，并求出导数 $A'(x)=20-4x$。
- 下一步：令面积导数为零，求最优尺寸并验证最大面积。
- 完成围栏优化：由 $x=5$ 得到 $y=10$，最大面积为 $50\text{ m}^2$。掌握“约束 → 单变量函数 → 求导 → 极值 → 回到实际量”的完整流程。
- 需要辨析优化逻辑：不是寻找导数的最大值，而是用导数判断原函数的极值；在原函数最大点，通常满足导数为 0 且符号由正变负。
- 下一步：把导数与积分连接起来，理解它们为什么互为逆过程。

**阶段 6：推开积分的门**

- 复习确认：对 $s(t)=t^2$，从 $t=2$ 到 $t=3$ 的平均速度为 $5$，而 $t=2$ 时的瞬时速度为 $s'(2)=4$；能够区分平均速度与瞬时速度。
- 理解曲线速度图的关键差异：当 $v(t)=t^2$ 时，曲线下面积表示总路程，不能只用初速度和末速度的平均值来代替；从 $0$ 到 $2$ 的路程写作 $\int_0^2 t^2\,dt=\frac83$。
- 当前进度：已经建立“导数描述某一瞬间变化多快、积分描述一段时间内累积多少”的直觉。
- 下一步：用许多窄矩形逼近曲线下面积，再正式理解定积分与导数为什么互为逆过程。
- 完成四个右端点矩形的估算：对 $v(t)=t^2,\ 0\le t\le2$，得到 $0.5(0.25+1+2.25+4)=3.75$；理解这是近似路程而非真实面积，并且由于曲线递增、矩形较宽而明显高估。
- 下一步：把区间分成 $8$ 个、$16$ 个更窄的矩形，观察估算值逐渐靠近真实面积 $\frac83$，再理解“切得无限细”的定积分。
- 理解矩形逼近的改进：区间切得更细时，右端点估算从 $3.75$ 降到 $3.1875$、$2.921875$，逐渐靠近真实面积 $\frac83$。
- 下一步：定义“从 $0$ 累积到 $T$ 的面积” $S(T)$，观察把 $T$ 增加一点时新增面积约为 $v(T)\Delta T$，从而得到 $S'(T)=v(T)$，正式连接积分与原函数。
- 当前出现模糊：还没有直观理解“累积面积函数的导数为什么等于当前速度”，暂时不把“积分与求导互为逆过程”视为已经掌握。
- 教学调整：先回到 $v(t)=2t$ 的三角形面积，用具体时间、总路程和当前速度的对应关系建立直觉，再返回曲线与定积分符号。

### 第二次学习：复攻“累积函数的导数”（2026-09-10）

- 回到 $v(t)=2t$：自己算出从 0 到 T 的累积路程 $S(T)=\frac{T\cdot v(T)}{2}=T^2$，并主动纠正“v-t 图是曲线”这个不准确的说法——它是直线。
- 认出 $S(T)=T^2$ 就是 A3 阶段玩过的 $y=t^2$：T=1、2、3 分别走了 1、4、9 米。
- 出现的混淆：把 $S(3)$ 写成 6，而 6 其实是 $v(3)$。也就是说，“到 T 为止攒下的总量”和“T 这一瞬间的速度”被写成了同一个东西——这恰好就是本关卡要区分的两个人。
- 尚待打通：“斜率”目前只被理解为“左右两点连线的平均斜率”，还没自然地把“某一点的斜率”当成一条随 T 变化的表达式；所以 $S$ 的斜率等于 $v$ 这一步仍未自己走出来。
- 下一步：先把 $S=T^2$ 在任意 T 的斜率写成表达式（$2T$），再与 $v(T)=2T$ 并排对照；若仍未通，按地图的止损规则换线走 B1（线性方程组）。
- 状态：A 线第 4 站（积分）仍为进行中，未过关。

---

### 第三次学习：重新评估（2026-09-11，一题一问）

- $f(t)=t^3$ 在 $t=0$：起初只会画图判断不是极值，不知与导数的关系；学到判据“导数为 0 只是候选，变号才是极值”，自己说出“左右斜率都是正、没变号，只是平了一下”。A3 复核通关。
- $(2t^3+4t^2-1)'=6t^2+8t$ 秒出，系数关已过。
- $v(t)=2t$ 从 0 到 3：初答 4.5，复盘发现把平均里的 $/2$ 算了两遍（$v(1.5)=3$ 已是平均速度，不用再除 2）；已统一三角形 $0.5\times3\times6=9$ 与平均 $(0+6)/2\times3=9$；分清 $S(3)=9$（总量，米）与 $v(3)=6$（速度，米/秒）是两个人；自己写出 $S(T)=T^2$ 的斜率为 $2T$，与 $v(T)=2T$ 对照，A4 通关。
- 二元方程组 $\begin{cases}x+y=10\\2x-y=5\end{cases}$ 秒解 $x=5,y=5$；“无解=平行无交点、无穷多解=重合”一次答对。B1 进行中。
- 下一步：B1 收口（三元消元 + 行列式为什么是面积），或按状态换 D3（单位圆三角函数）。

---

## 待办（下次学习从这里开始）

1. B1 收口：三元方程组消元 + $2\times2$ 行列式为什么等于面积。
2. 碎片时间：幂函数求导保持秒出。
3. 可选项：D3 单位圆上的正弦余弦（零微积分前置）。
